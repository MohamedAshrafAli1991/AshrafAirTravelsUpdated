import { CounterSinppetDirective } from './counter-sinppet.directive';

describe('CounterSinppetDirective', () => {
  it('should create an instance', () => {
    // const directive = new CounterSinppetDirective();
    // expect(directive).toBeTruthy();
  });
});
